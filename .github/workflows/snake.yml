name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    # ضيف الجزء ده هنا عشان تسمح للأكشن إنه يرفع الملفات
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Generate snake
        uses: Platane/snk@v3
        with:
          github_user_name: MohamedAyman-11
          outputs: |
            github-contribution-grid-snake.svg
            github-contribution-grid-snake-dark.svg?palette=github-dark

      - name: Push to branch
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "generate snake"
